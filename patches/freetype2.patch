--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -202,6 +202,12 @@ add_library(freetype
   ${BASE_SRCS}
 )
 
+if (MSVC)
+    set_target_properties(freetype PROPERTIES
+        OUTPUT_NAME freetype${VERSION_MAJOR}${VERSION_MINOR}${VERSION_PATCH}MT
+    )
+endif ()
+
 if (BUILD_FRAMEWORK)
   set_property(SOURCE ${PUBLIC_CONFIG_HEADERS}
     PROPERTY MACOSX_PACKAGE_LOCATION Headers/config
